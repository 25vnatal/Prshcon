# Parish Connect — Screens Index

Design system: **Community Heart v1** (locked — supersedes "Midnight Digital v1"). Source files: `design-tokens.css`, `head-snippet.html`, `style-guide.html`.

Every screen below must:
- Use the exact `head-snippet.html` block in its `<head>`
- Pull components from `style-guide.html` rather than inventing new markup
- Implement empty / loading / populated / error states
- Reflect the Family → Member data model without duplicate entry

| File | Interface | Flow | Status | Notes |
|---|---|---|---|---|
| `style-guide.html` | Shared | Design system reference | ✅ Done (Community Heart) | Not a product screen — component library only |
| `index.html` | Shared | Demo launcher | ✅ Done (Community Heart) | Links to each interface's login |
| `screen-parishioner-login.html` | Parishioner | Login | ✅ Done (Community Heart) | Realistic email/password form, accepts any non-empty input |
| `screen-parish-login.html` | Parish | Login | ✅ Done (Community Heart) | Parish picker (all 130 parishes); non-seeded parishes → empty dashboard, proving data isolation. **iPad fix**: the parish picker didn't work as a picker on iPad — traced to the root cause rather than guessing: it was built as a native `<input list="...">` + `<datalist>`, and Safari on iPadOS has notoriously unreliable `<datalist>` rendering (the suggestion panel frequently doesn't appear on tap at all, even though the identical markup works fine on Chrome/desktop). Replaced with a custom-built searchable dropdown — a text input filtering a visible, tap-target-sized list of results (44px min-height rows, matching the touch-target standard used throughout this app), selection via a real `onclick` handler rather than relying on any native picker UI, a clear (×) button once a parish is selected, and click-outside-to-close. Capped to showing 8 results at a time with a "keep typing to narrow" hint for broader queries, consistent with the same pattern used for long lists elsewhere in this app (Diocese Settings' Parish Management table, for instance). Verified via script test: filtering, selection, clearing, the no-match state, and that the login form's validation (`findParish()`) correctly uses the exact picker-selected parish rather than re-running its fuzzy text match once a real selection has been made — and confirmed zero remaining references to `datalist` anywhere in the file |
| `screen-diocese-login.html` | Diocese | Login | ✅ Done (Community Heart) | Single diocese-admin account; brand panel uses deep blue, not black, per "no dark backgrounds" rule |
| `screen-parishioner-dashboard-home.html` | Parishioner | Dashboard (home) | ✅ Done (Community Heart) | Reordered for concision: personalized content leads, Quick Actions/Announcements/Community lower. Slim horizontal **Hero banner** (no pill), CTA reads "View Details". Greeting slimmed (no parish subtext). **Upcoming** (renamed from "Your schedule"): rows show mass timing, then church directly below, then intention detail (if any) on its own line — no combined subtext. **My Requests** card: left "My Requests" label, right "N updates" + chevron, sized identically to the Other Actions card (p-4, min-h-[36px] on both). **Other Actions** swapped Feast RSVP for a Subscription payment reminder, count badges read "N pending." Coral bell icons + a red dot on the avatar flag anything actionable. **Parish Announcements** (renamed) shows parish name next to the week. **Search**: chips only pre-fill the bar (via `fillSearch`); results only appear on Enter or the explicit Search button, headed "Results for '[query]'". **Logo fixed**: was JPEG data mislabeled `.png` (MIME mismatch caused a broken-image icon on strict hosts) — replaced with the text-free logo saved correctly as `assets/pc-logo.jpg`, now styled square (`rounded-lg`) instead of circular. **Upcoming** now collapses to 1 tile by default with a "Show N more" expand toggle; Mass Intention date changed to 9 Aug to differ from Next Mass's 2 Aug. Section headers reduced from `text-lg` to `text-base`. Quick Actions tiles made more compact (smaller padding/icon) |
| `screen-parishioner-documents-request.html` | Parishioner | Documents & Certificates → Request Document | ✅ Done | Guided multi-step flow; "Details" step (renamed from Supporting Documents) now shows per-document-type auto-filled fields (from family/member record) plus parishioner-input fields (Godfather/Godmother, Witnesses/Celebrant, Destination, etc.) per the product owner's field list, alongside supporting doc upload. Delivery step: renamed/clarified options, each expands in-place within its own card (not a separate block) to capture email/address/pickup details |
| `screen-parishioner-documents-myrequests.html` | Parishioner | Documents & Certificates → My Requests | ✅ Done | Status list with filter tabs (New/In Progress/Awaiting Action/Completed), expandable Activity Timeline per request using the granular→display status mapping from mock-data-schema.md. **Sorted by most-recently-updated first** (`lastUpdated` field); requests updated within the last 3 days get a left accent border + "UPDATED" badge so they're visually distinct, not just reordered. 5-item bottom nav added (Services highlighted) |
| `screen-parishioner-documents-mydocuments.html` | Parishioner | Documents & Certificates → My Documents | ✅ Done | Completed/delivered documents only, simulated download; empty state if none. 5-item bottom nav added (Services highlighted) |
| `screen-parish-requests-hub.html` | Parish | Requests hub → Documents & Certificates | ✅ Done | Mini-workflow checklist per request (Verify → Prepare & upload certificate [captures Certificate Number + Issue Date] → Request Approval [note shown directly, no extra reveal click] → Priest approval → method-specific delivery action). Tabs: All/Pending/Awaiting Approval/Deliver/**Completed** (renamed from Archive). Paid pill now sits in the card header; standalone Verification pill removed (conveyed via the Verify workflow step instead). Detail view shows per-document-type "Certificate details" (auto-filled + parishioner-provided fields). Completed requests show their completion date. One request (Marriage Certificate) demonstrates the Post/Courier delivery flow. **New Request** button opens a simple one-page form (not a wizard) for staff to capture walk-in/offline requests. **New "Family Updates" tab** (fixing a discoverability gap — these requests previously only lived inside the Family Records drawer, easy to miss): mirrors the same seed data as Family Records' Updates tab, shows a pending-count badge on the tab itself, and each row deep-links to `screen-parish-familyrecords-search.html?family=ID&tab=updates` — the actual approve/reject workflow stays in Family Records as the single source of truth (so the family record edit and the approval stay in one place), this tab just makes the requests visible from where staff would naturally look for them. Verified via a sandboxed script test: tab renders the real request data, shows the correct Pending status, and the deep-link carries the right family ID. Supports `?module=familyupdates` to open directly to this tab (used by the Dashboard's Action Center "Family updates" tile, which was updated to route here instead of straight to Family Records). **New "Sacraments" tab** — closes a real gap found in an audit: parishioners could already submit Sacrament registrations via the Parishioner-side flow, but staff had no way to see or act on them (the tab was a "not built yet" placeholder). Built as a genuinely lighter workflow than Documents' (no file generation or delivery methods needed) — an expandable card per registration showing required documents, registration fee/payment status, and any additional info from the parishioner, with **Approve** or **Reject** (rejection requires a reason, shown to the parishioner, matching the validation pattern used elsewhere in this app). Tabs: All/Submitted/Approved/Rejected, with a pending-count badge on the tab itself and `?module=sacraments` deep-link support, both mirroring the Family Updates tab's established conventions. Verified via script test: tab filtering, expand/collapse, approve, and reject-blocked-without-a-reason all confirmed working; badge count and deep-link both confirmed correct on page load. **New "Home Services" tab** (Batch 2 of the same audit fix) — same gap, same fix pattern, adapted for a genuinely different lifecycle: Pending Approval → Scheduled → Completed (no payment step, since home services are free). "Approve & Schedule" captures a date/time via inline inputs pre-filled with the parishioner's preferred slot but editable by staff, separate from the later "Mark Completed" step once the visit has actually happened; Reject requires a reason, same as Sacraments. Emergency requests still in Pending Approval get a visible red ring around the card so they don't get lost among routine ones. Tab badge and `?module=homeservices` deep-link support, same conventions as before. Verified via script test: schedule is blocked without both a date and time, succeeds and formats the date correctly once provided, complete and reject-with-reason both work, tab filtering correctly isolates Completed vs. others, and the emergency-ring highlight was confirmed to fire correctly when the actual condition (emergency + still pending) is met — the seed data's one emergency case happened to already be Scheduled, so the negative test result there was verified to be correct behavior, not a missed bug, by re-testing against a case that does meet the condition. **New "Facility Bookings" tab** (Batch 3, completing the audit fix — all three previously-missing Requests tabs are now real): Pending Approval → Confirmed, or Rejected — no scheduling step needed since the parishioner already picked a specific date and slot when booking, closer in shape to Sacraments (has a fee/payment status) than to Home Services. Detail view shows expected attendees, purpose, and the booking fee/payment status; Confirm or Reject-with-reason, same conventions as the other two. Tab badge and `?module=facilities` deep-link support. **The Requests hub is now functionally complete against the Nav Hierarchy spec** — every request type a parishioner can submit has a real staff-side workflow to process it; no more silent dead ends. Verified via script test: tab filtering, expand/confirm/reject-blocked-without-reason all work correctly, and the pending-count badge correctly decrements after a booking is confirmed |
| `screen-parish-documents-hub.html` | Parish | Documents | ✅ Done | **New module, scoped from the DOC-ISD PRD set** (Certificate & Document Requests / Document Workspace / Document Templates / Document Service) down to what was actually asked: staff can view templates and generate certificates from them. Positioned right after Requests, since certificate generation is what fulfils an approved document request. Three tabs. **Templates** (DOC-ISD-003, view-only — template design/authoring is out of scope): 6 templates seeded to match the document types already configured in Services → Documents & Certificates (Baptism/First Communion/Confirmation/Marriage Certificate, NOC, Parish Transfer Certificate), grouped by category, each showing its guided fields and raw placeholder body text in a detail modal. **Create Certificate** (DOC-ISD-002, simplified Document Workspace): pick a template, fill a guided form on the left, watch a live-merged certificate preview update on the right as you type — deliberately simplified from the full spec (no rich-text manual editing of the preview, no Reset-to-Template, no Word export) since those add real complexity for a prototype without changing the core "generate a certificate from a template" value being asked for; Save as Draft / Generate Certificate / Print / Email actions, the last two toast-simulated. **Generated Documents**: a log of everything generated, with reference numbers. Sidebar link added to all 11 other Parish screens (inserted right after Requests everywhere) with a `goDocuments()` helper; canonical sidebar order re-verified identical across all 12 Parish screens after insertion. Verified via script test: template selection correctly defaults field values (e.g. Church pre-fills to the parish name), the live preview merges every field into the body with dates properly formatted and zero leftover `{{placeholder}}` tokens, and generating a certificate correctly logs it and resets the form . **Create Certificate workspace redesigned per direct feedback**: layout changed from an even 50/50 split to 40/60 (guided fields : preview), giving the certificate itself more visual room to read as the actual output rather than a secondary panel. The preview container is now a fixed 560px height with internal scroll, so it no longer visibly grows or shrinks as fields are filled in — a real usability improvement, since the old auto-height version would shift the buttons below it on every keystroke. The certificate itself was rebuilt to actually look like one rather than styled body text: added Playfair Display (a serif display font) for the heading and signature, a gold double-border frame with four corner accents around the whole preview, a small circular seal-and-laurel emblem above the parish name (inline SVG, no external image dependency), a decorative divider beneath the title, and a "Certificate of {Sacrament}" heading generated from each template's own name — verified via script test across all six templates (Baptism/First Communion/Confirmation/Marriage/No Objection/Parish Transfer) that the title renders correctly for every one, not just the template used during development. Verified via script test: the merged certificate content still shows real field values with dates formatted and zero leftover placeholder tokens, exactly as before the redesign — the visual overhaul didn't touch the underlying merge logic |
| `screen-parish-reports-hub.html` | Parish | Reports | ✅ Done | Five tabs following the Nav Hierarchy structure (Operational/Financial/Sacramental/Audit/Export), each built with genuinely computed-feeling numbers rather than placeholder zeros — figures were chosen to stay internally consistent with the rest of the app (e.g. Registered Families 1,842 and Active Parishioners 6,120 match the Dashboard's own Parish Snapshot; Total Monthly Payroll ₹86,000 matches the sum of Finance's actual `payrollStaff` seed data). Simple CSS-only horizontal bar charts (no charting library added, to stay consistent with the rest of the app's dependency-free approach) used throughout instead of static tables alone. **Operational**: registered families/parishioners/new-registrations/turnaround stat cards, requests-by-type and requests-by-status bar charts, a 6-month request volume trend, and a committees summary. **Financial**: income/expense/net/collection-rate stat cards, income-by-category and expense-by-category bar charts (matching Finance's actual category list), a 6-month net-position trend, and a payroll summary. **Sacramental**: YTD counts for all four sacraments, a Baptisms-by-month trend, a year-over-year comparison chart, and a table with computed % change per sacrament vs. the same period last year. **Audit**: a filterable log (by action type and by date) of realistic actions tied to the app's own narrative — approvals, an announcement publish, a staff status change, a payment record, a new family registration, a certificate generation — each with actor, timestamp, and detail. **Export**: report type + date range + PDF/CSV format, with a working "Recent Exports" log that new exports get added to. Sidebar link added to all 12 other Parish screens with a `goReports()` helper; canonical order re-verified identical across all 13 Parish screens. Verified via script test: all five tabs render substantial real content, audit filtering by both action and date correctly narrows the list, and running an export adds a new correctly-formatted entry to the recent-exports log |
| `screen-parish-settings-hub.html` | Parish | Settings | ✅ Done | Four tabs following the Nav Hierarchy structure. **Users & Roles & Permissions**: 5 seeded accounts reusing real names already established elsewhere in the app for continuity (Fr. John Anthony, Fr. Francis Billemane, Grace Almeida, Thomas Rebello, Peter Vaz) — deliberately distinct from Administration's Staff directory, since this is about *system access* (email/role/permission level/status), not HR records. Invite/Edit via modal; status changes (Disable / Re-enable) go through the same kebab-menu-plus-inline-confirm pattern established for Administration's Staff, reusing the fixed-position dropdown technique to avoid the table's `overflow-hidden` clipping it. **Notification Settings**: channel toggles (In-App locked on, Email/WhatsApp/SMS toggleable) plus a per-event × per-channel matrix (Request Submitted/Approved/Rejected, Document Ready, Payment Received, Announcement Published) — matches the notification model described in the Documents PRD; a channel's event-checkboxes are disabled until that channel itself is turned on. **Payment Settings**: accepted-methods checkboxes (UPI/Card/Net Banking/Cash/Bank Transfer/Cheque), an online-payment-required toggle, and read-only gateway/bank details (masked, matching how sensitive config is typically displayed rather than edited inline). **System Configuration**: timezone/currency/date format/fiscal year/language — kept intentionally minimal per "Minimal Configuration," since these rarely change once set. Sidebar link added to all 13 other Parish screens with a `goSettings()` helper — **this completes the sidebar**: every item in the canonical order is now a real, built screen except none remain as placeholders. Hit and fixed a real bug during the build: a heredoc-escaping mistake left several `onclick` handlers with broken nested-quote JavaScript that would have thrown a runtime error — caught by the standard syntax-validation pass before it shipped, not by chance. Verified via script test: all four tabs render, the user kebab/confirm flow correctly gates a status change behind explicit confirmation, and both notification-channel and payment-method toggles flip their underlying state correctly |
| `screen-parish-committees-hub.html` | Parish | Committees | ✅ Done | Full CRUD on both Committees and Committee Members. List view: searchable/filterable cards (by name/member name, status All/Active/Inactive), each showing office-bearer and member counts. Right slide-in drawer (same pattern as Family Records) shows full committee detail split into **Office Bearers** (President/Vice President/Secretary/Joint Secretary/Treasurer — sorted by seniority) and **Members** sections, derived automatically from each member's `role` field rather than a separate flag. Add/Edit Committee and Add/Edit Member both via modal; committee deletion requires an inline confirm step, member removal is immediate. Seeded with 6 real committees (Parish Pastoral Council, Finance, Choir, Ladies' Guild, Youth, Building & Maintenance) populated with existing family names for continuity. Sidebar "Committees" link wired up across all Parish screens (was a `toast()` placeholder everywhere before this). Verified end-to-end via a sandboxed script test: add/edit/delete committee, add/edit/remove member, and office-bearer/member reclassification on role change all confirmed correct |
| `screen-parish-finance-hub.html` | Parish | Finance | ✅ Done | Kept intentionally lightweight per schema section 4.9 — skipped the Reports section, Draft/Pending Approval workflow states, and a separate paymentStatus field; just Posted/Voided. Three tabs: **Income** and **Expenses** (shared table renderer filtered by type — Date/Category/Description+reference/Account/Amount/Status/Edit-Delete), and **Categories & Accounts** (two-column CRUD lists with **delete blocked and a toast shown** if a category or account is still referenced by an existing transaction). Summary cards (Total Income/Total Expenses/Net Balance) computed from Posted transactions only. Seed data ties into the rest of the app's narrative: Mass-offering income entries reference real booking numbers from the Masses module, a subscription income entry references the Rodrigues family's Family Records subscription, and an expense entry ties to the Parishioner Dashboard's weekly announcement. Sidebar "Finance" link wired up across all Parish screens (was a `toast()` placeholder everywhere). Full CRUD verified via a sandboxed script test: add/edit/delete on transactions, categories, and accounts, plus confirmed the delete-blocked-when-in-use guard correctly prevents removing a category/account still in use while allowing unused ones to be deleted. **Restructured**: "Categories & Accounts" demoted from a primary pill tab to a secondary text link ("Manage Categories & Accounts"), since it's configuration rather than a day-to-day workflow; opens into its own Categories/Accounts sub-tabs, with Categories now visually split into Income and Expense columns instead of one undifferentiated list. **New Payroll tab** (moved here from Administration, since salary/payment details belong in Finance, not the staff directory) — Total Monthly Payroll summary plus the tap-to-toggle Paid/Pending pill per staff member that used to live in Administration. Verified via script test: Payroll toggle flips status correctly, and both settings sub-tabs render their expected content. **Manage link simplified into a dropdown**: the "Manage Categories & Accounts" text link required an extra click through an intermediate sub-tab bar to actually reach either view — replaced with a "Manage" button that opens a small dropdown offering **Categories** and **Accounts** directly, landing straight on the chosen one. **Payroll toggle now requires confirmation**: tapping the Paid/Pending pill directly changed payment status on a single click, which was too easy to trigger by mistake — reworked to the same confirm-step pattern used for Staff status changes elsewhere (tap shows "Mark as Paid/Pending?" with Confirm/Cancel, no change happens until Confirm is tapped). Verified via script test: the dropdown navigates directly to the correct sub-view, and confirmed status genuinely does not change until the explicit Confirm step. **New Donation Causes tab, closing a real gap found during a cross-side feature comparison**: the person asked how a parish creates a donation cause and there was genuinely no way to — the two causes parishioners could give to were hardcoded directly in `screen-parishioner-donate-hub.html` with no admin-side management screen, and individual donations were completely invisible to staff beyond a single aggregate "Donations" line in the Income ledger. New tab lets a parish create/edit a cause (name, description, Goal-based-with-target or Open-ended, suggested amounts) and close/reopen it, plus a genuine donations log (donor, family, cause, amount, date, payment method) — something that didn't exist in any form before. Kept consistent with the Parishioner side per the established Option A convention (same seed data in both files, no live backend to sync from) — the two causes and their raised totals match exactly what's shown on the Parishioner Donate hub, and two of the five seeded donations (Rodrigues family, same amounts and dates) are the exact same gifts already referenced there, so a person clicking through both sides sees a consistent story rather than two disconnected datasets. Deliberately did not try to make the donations log sum to each cause's full raised total — a fund with ₹16+ lakh raised realistically has far more individual gifts than are worth hand-listing in a demo, so it's labeled as a recent sample rather than a complete ledger, avoiding a false completeness claim. Verified via script test: cause creation, editing an existing cause without duplicating it, and the close/reopen toggle all behave correctly — including catching and correcting a mistake in my own first test assertion (I initially expected the wrong cause count after an edit and had to verify against the actual, correct app behavior before concluding what I'd built was right) |
| `screen-parishioner-faithformation.html` | Parishioner | Faith Formation | ✅ Done | **Batch 2 of the cross-side gap-closing plan.** The Nav Hierarchy spec's Dashboard "Spiritual Segment" (Today's Gospel / Reflection / Prayer of the Day / Feast Day) had never actually been built — checked the Dashboard specifically and found only one static hardcoded line buried in a search-result branch, not a real section, despite Parish (and Diocese) having a fully working content-management screen publishing real prayers and reflections with nowhere for a parishioner to ever read them. New dedicated screen with search, a 5-category filter (matching Parish's exact categories: Daily Prayers/Readings & Reflections/Special Prayers/Rosary/Feast Day Content), and tap-to-expand cards. **Deliberately reuses the exact same content already published on `screen-parish-faithformation-hub.html`** rather than inventing a separate set — 10 of its 12 pieces (the 2 Drafts correctly excluded, since a parishioner shouldn't see unpublished content), so what a parish actually publishes is precisely what a parishioner reads, not two independent copies that happen to look similar. Also added a compact "Today" card to the Dashboard itself — positioned right after the hero banner, ahead of "Things needing your attention," matching the Nav Hierarchy's intended ordering (Spiritual Segment second, right after Global Search) rather than the ad-hoc placement it might otherwise have gotten — featuring today's Prayer of the Day with a tap-through to the full screen. All original content, not scripture quoted from any specific copyrighted translation. Verified via script test: exactly 10 items present (not 12, confirming the Draft exclusion), category filtering isolates the right subset (Rosary shows exactly 3, correctly excludes Daily Prayers content), search correctly narrows to matching titles/body text, and the expand/collapse toggle correctly shows and hides the full body text on repeated taps |
| `screen-parishioner-events.html` | Parishioner | Parish Events | ✅ Done | **Batch 3.** Parish-side Communication hub genuinely tracks event registrations — some Published events already had real registrant data seeded — but no screen anywhere let a parishioner actually register. Two tabs: **Upcoming** (the same 4 Published events already on the Parish side — Feast Day Celebration, Youth Retreat 2026, Winter Clothes Drive, Christmas Celebration — the 5th event, Marian Procession, correctly excluded since it's still Draft there) with a bottom-sheet RSVP flow (name pre-filled, attendee count, the paid event's ₹500 fee shown as payable at the venue rather than charged in-app, consistent with how this app hasn't built in-app event payment anywhere else); **My Registrations** showing what's been confirmed. The Dashboard's hero event card ("St. Mary's Basilica Feast") was deliberately left alone rather than wired into this flow — it's a different, non-Infant-Jesus-Shrine event, more of a diocese-wide/community item than something with a real registration record behind it, so forcing it into the RSVP flow would have been a worse fix than leaving it as-is. Verified via script test: exactly 4 events present (Draft correctly excluded), the paid event's fee renders, registering updates both the event's own card (shows "You're registered") and the My Registrations tab with the correct attendee count |
| `screen-parishioner-committees.html` | Parishioner | Committees & Ministries | ✅ Done | **Batch 4.** Parish-side Committees hub manages 6 real committees with full membership — nothing on the Parishioner side showed any of it, so there was no way to know who ran a ministry or how to get involved. Reuses the same 5 **Active** committees (the 6th, Building & Maintenance, is Inactive on the Parish side and correctly excluded — not something worth surfacing to parishioners), showing office bearers only rather than the full member roster, since that's the level of detail a parishioner actually needs versus what staff manage internally. "Express Interest" opens a lightweight note-and-submit flow rather than a full volunteer-application form, appropriately scaled to what this action actually needs. Verified via script test: exactly 5 active committees shown, office bearers and roles render correctly, and the interest flow correctly identifies which committee was selected in its confirmation |
| `screen-parishioner-parish-profile.html` | Parishioner | Parish Profile | ✅ Done | **Batch 5, the smallest of the five** — a read-only page pulling together patron saint, feast day, year established, motto, clergy, address, and office hours, all sourced from the exact same data already established on `screen-parish-administration-hub.html`'s Parish Profile tab rather than invented separately. Closes out the last of the 5 gaps found during the cross-side feature comparison. **This is also what "Opening Parish Profile…" on the Dashboard had been toasting since a much earlier audit** — fixed as part of this batch along with adding two more links (Parish Events, Committees & Ministries) to the same "My Parish" section, so all three of this round's new screens are reachable from one place rather than needing separate, easy-to-miss entry points. Verified via script test: real clergy names, phone number, and parish name all present and correct, and confirmed the Dashboard's old toast placeholder is completely gone rather than just supplemented |
| `screen-parish-administration-hub.html` | Parish | Administration | ✅ Done | Only **Parish Profile** has a documented schema (mock-data-schema.md section 4.10) — the other two remaining sub-modules (Inventory, Staff & Payroll) are named in the Navigation Hierarchy only, so those were designed pragmatically and kept lightweight per "Minimal Configuration." **Parish Profile**: view/edit toggle on basicInfo/contactInfo/address/officeHours/aboutParish (pre-filled from the real seed data in `mock-seed.json`), plus separate mini-CRUD (via modal) on **Clergy** and **Worship Locations** since those are arrays within the profile; a read-only "At a glance" sidebar surfaces aggregated info from other modules (this week's Masses, recent announcements, ministries/committees, upcoming events, home services, giving options) exactly as the schema specifies "sourced from other modules, read-only." **Inventory**: CRUD list (item, category, quantity, condition, location). **Staff & Payroll**: CRUD list plus a tap-to-toggle Paid/Pending pill per staff member for the current month, with a Total Monthly Payroll summary card — deliberately not a full payroll-processing system. **Document Templates removed from here** — moved and enriched into the new Services hub (see below), following the audit that found it too thin to actually drive the request flow. Sidebar "Administration" link wired up across all Parish screens. Full CRUD verified via a sandboxed script test. **Staff view reworked**: removed Monthly Salary and the "This Month" payment toggle entirely — those were confusing here and belong in Finance, not a staff directory (see Finance's new Payroll tab). Replaced hard **Delete** with **Mark Inactive** / **Terminate**, each requiring an inline confirm step before changing `employmentStatus` — no more silently removing a staff record. Verified via script test: both status transitions correctly update the record, and the removed columns are confirmed absent from the rendered table. **New Services link** added to the tab row (visually separated with a divider, styled as an outlined pill with an external-link icon rather than the filled active/inactive tab style) — clicking it navigates to `screen-parish-services-hub.html`, since Services was demoted out of the primary sidebar and now lives here. **Staff tab renamed** "Staff & Payroll" → **"Staff"** (payroll content already lives in Finance, so the tab label was stale). **Actions consolidated into a kebab menu**: the always-visible Edit icon plus a red Terminate button sitting right in the row were too prominent for infrequent actions — replaced with a single kebab (⋮) button per row that reveals Edit / Mark Inactive / Terminate in a dropdown (Terminate still shown in error-red text, but no longer a standing button). The dropdown is rendered via `position: fixed` computed from the button's actual screen coordinates and appended to `<body>` rather than nested inside the table — the table's card wrapper has `overflow-hidden` for its rounded corners, which would otherwise clip an absolutely-positioned dropdown for any row (a real bug caught and fixed before shipping, not just a hypothetical). Verified via script test: kebab opens/closes correctly, menu contains all three actions, closing removes the DOM node cleanly, and triggering Terminate still goes through the existing confirm step before actually changing status (not an immediate action) |
| `screen-parish-services-hub.html` | Parish | Services | ✅ Done | **New module, built from an IA audit finding**: the app captured parishioner requests for Sacraments, Home Services, and Documents & Certificates, but had nowhere to configure what a parish actually offers for any of them — Sacraments and Home Services had zero config UI anywhere, and Documents & Certificates only had a thin name/type/status list buried in Administration. Positioned in the sidebar between Masses and Requests (config before the transactions that consume it), named "Services" to mirror the Parishioner side's own "Services" hub — same domain, config vs. request. Four tabs, each following the config-list-plus-modal pattern already proven out by Masses' "Manage Parish Masses": **Sacraments** (schema 4.3 — registration mode Always Open/Window, min age, parish membership requirement, required documents, fee, instructions; seeded with Baptism/First Communion/Confirmation/Marriage); **Home Services** (schema 4.2's fixed list of 8 types — Home Communion, House Blessing, Sick Visit/Anointing, Funeral Visit/Prayer, Confession at Home, Family Prayer/Rosary, Blessing of Vehicle/Business/Office, Other Pastoral Visit — each with availability, approval-required and emergency-allowed flags); **Documents & Certificates** (schema 4.1, enriched from Administration's old thin version — fee, required supporting documents, delivery options, processing time); **Facilities** (no documented schema, designed pragmatically — name, type, capacity, booking fee, availability). Sidebar link added to all 7 existing Parish screens (inserted between Masses and Requests everywhere), each with a `goServices()` helper. Full CRUD verified via a sandboxed script test across all four tabs. **Facilities reworked** to support real availability configuration: `availableDays` (checkboxes per day of week — some facilities can now genuinely be unavailable on Sundays) and a `slots` array (named booking sessions like Morning/Evening, each with its own start/end time, add/remove via the edit form) — replacing the old free-text "availability" string. Verified via script test: adding a facility with 2 days selected and 2 slots produces the correct structured data. **Removed from the primary sidebar entirely** — per feedback that a one-time/slowly-changing config surface didn't warrant that much prominence. Now reached via Administration's tab row (a distinctly-styled link with an external-link icon, since it navigates away rather than switching an in-page tab) rather than its own sidebar item; the page itself now shows an "Administration / Services" breadcrumb and highlights Administration as active in its sidebar, since it's conceptually a sub-page. Verified via script test that both directions of the link resolve correctly |
| `screen-parish-communication-hub.html` | Parish | Communication | ✅ Done | Three tabs (Faith Formation extracted out into its own top-level screen, see below). **Announcements** (schema 4.5): reworked per feedback — **title suggestion chips** ("Announcements for the week of", "Feast day schedule", etc.) in the Add/Edit modal; **status is action-driven** (Save as Draft / Publish buttons, no status dropdown); **modal widened** (max-w-md → max-w-2xl) and now supports **attachments shown inline** in the announcement itself rather than requiring a download (simulated upload — add/remove chips in the form, rendered as tags on the card); **search by title/text and by date** in the toolbar. **Circulars** (no documented schema, designed pragmatically): title, category, attachment file name, status — flat table matching the density of the Finance/Administration tables. **Events** (schema 4.7): added Outreach and Celebration categories plus three new seed events (Winter Clothes Drive, Christmas Celebration, alongside the existing Feast Day Celebration and Youth Retreat); **registration capacity can now be left uncapped** (0 = "no limit", shown explicitly on the card rather than just omitting a number) — Feast Day Celebration and Christmas Celebration both use this, since a parish-wide feast shouldn't have an artificial headcount limit. Registrations modal (tap-to-toggle Present/Not Recorded attendance) unchanged. Sidebar "Communication" link wired up across all Parish screens. Verified via script test: search and date-filter both narrow the list correctly, attachment add/remove works, Draft→Published transition sets the publish date, and uncapped events correctly show "no limit" while capped ones show their real number |
| `screen-parish-faithformation-hub.html` | Parish | Faith Formation | ✅ Done | **Promoted out of Communication into its own top-level nav item** (positioned right after Communication) — the reasoning: this is a distinct, ongoing body of devotional content with its own categories and audience, not a single announcement, so burying it as a fourth tab undersold it. Two tabs: **Content** and **Media & Videos**. **Content**: now organized into real categories (Daily Prayers, Readings & Reflections, Special Prayers, Rosary, Feast Day Content — replacing the old flat Daily Readings/Reflections/Prayer Content/Feast Day Content list), with a **search bar** (matches title and body text) plus a category filter dropdown, both combinable. **Status is action-driven** (Save as Draft / Publish, not a dropdown). Seeded with 12 real pieces of content across all five categories (up from 3) — Morning Offering, Grace Before Meals, a Sunday Gospel reflection, prayers for the sick and for vocations, all three sets of Rosary Mysteries plus a beginner's how-to-pray guide, and feast day content. **New Media & Videos section** for children/youth content — title, audience (Children/Youth/Both), duration, description, status — seeded with 4 items (a kids' Bible story, a Rosary video guide, a youth talk recording, a Sunday School sing-along), file upload simulated per prototype convention. Sidebar link added to all 10 other Parish screens with a `goFaithFormation()` helper; canonical sidebar order re-verified identical across all 11 Parish screens after insertion. Verified via script test: search and category filter both narrow the content list correctly (and combine), status-by-action sets Draft/Published correctly on both Content and Media items |
| `screen-parish-dashboard-home.html` | Parish | Dashboard (home) | ✅ Done (Community Heart) | Sidebar nav, session-aware; non-seeded parish logins render genuine Empty state, proving data isolation. "Register New Family" quick action (and the Empty-state "Register a Family" button) now link to Family Records with `openRegister=1`, auto-opening the Register modal there. Parish Connect logo added to the sidebar header, styled square (`rounded-lg`). **Body reimagined** against a reference dashboard image, sidebar left untouched: replaced the itemized "Things needing your attention" list with an **Action Center** — a row of count-tiles (Document requests/Sacrament approvals/Family updates/Home service requests/Payments awaiting), each with an icon, count, status word, and chevron, drilling through to the relevant module (real link for Document requests → Requests hub and Family updates → Requests hub's new Family Updates tab (updated after that tab was added — previously deep-linked straight to Family Records, bypassing it); the rest toast since those modules aren't built yet). Below that: **Parish Snapshot** (4-metric grid + "View all insights") and **Quick Actions** (icon row) stacked in the left column, **Today's Calendar** as a dot-marker timeline in the right column, and a horizontal **Recent Activity** strip full-width at the bottom. Added a notification bell with badge count to the header, replacing an unused search button. Verified via a script test that all four states (loading/empty/populated/error) render without runtime errors or `undefined` leaking into the output. **Polish pass**: found and fixed the same missing-base-`.icon`-class bug (9 occurrences) in Action Center, Parish Snapshot, and Recent Activity — icons were rendering as solid black shapes instead of clean colored line icons, which was the main cause of the dashboard looking rough in a review screenshot. Greeting section rebuilt into a proper hero banner with a soft gradient wash and a larger illustration. Action Center status copy warmed up ("Awaiting" → "Awaiting review" / "Awaiting your approval", "Verification" → "Needs verification"). **Card-layout pass**: compared each card type against the reference image directly and fixed two genuine structural mismatches — Parish Snapshot cards were left-aligned, reference has them center-aligned (icon/number/label all centered); Recent Activity combined subtitle and timestamp onto one line ("Subscription · 2 hours ago"), reference keeps them as two separate lines. Also bumped icon and number sizes across Action Center and Snapshot for more visual weight, matching the reference's more prominent numbers. Icon shape kept circular (`rounded-badge`) rather than switching to the reference's rounded-square, to stay consistent with the icon-badge convention used everywhere else in the app. Verified via script test that the rendered output contains the center-aligned classes and the two-line activity markup. **Stale links fixed (Batch 4 of the Requests-hub audit arc)**: "View full calendar" was still calling `toast('Calendar')` despite `screen-parish-calendar-hub.html` having existed for several turns by this point — a genuinely forgotten wire-up, now calls the already-defined `goCalendar()`. "Sacrament approvals" and "Home service requests" Action Center tiles had `go: null` since those Requests hub tabs didn't exist yet — now that they do, both tiles deep-link there via new `goSacramentApprovals()` / `goHomeServiceRequests()` helpers using the same `?module=` convention as the existing Family Updates tile. **"Payments" tile deliberately left as a toast placeholder** — there's no single existing screen that actually represents "payments awaiting verification" across all request types, so linking it to the nearest approximation (e.g. Finance) would have been a worse experience than an honest placeholder; flagged in the audit as lower-priority for exactly this reason. Verified via script test: all three fixed links resolve to the correct URLs, and the actual rendered HTML (not just the underlying data) was checked to confirm the onclick attributes are correct with zero `undefined` leaking anywhere. **Full redesign, mocked up first before touching code**: the person flagged five concrete problems with a screenshot of the live dashboard (broken-looking Action Center cards with dead whitespace and wrapped numbers, an oversized hero banner, questionable value of a full Quick Actions section, no way to see Diocese communication, general clutter) — explored via three initial directions in the Visualizer, then iterated through several more targeted variants (dropping Quick Actions as a full section per feedback, correcting the hero stats, restoring Today's Schedule which one iteration had dropped, testing three different Action Center card treatments, then a hover-lift animation and fixed-height labels) before implementing anything, matching the person's established "design alignment before building" preference. **Final implementation**: hero banner shrunk from a large gradient-and-illustration block to a slim single-row card with 3 inline stats (Families/Parishioners/Subscription paid — Pending requests dropped per feedback); Action Center cards redesigned with a bigger icon, a 2-3 word wrapped label with a fixed `min-height` so the number always starts at the same y-position regardless of label length, and a `hover:-translate-y-0.5` lift to signal clickability; all five labels relabeled since single words like "Sacraments" and "Payments" were ambiguous out of context ("Sacrament approvals", "Payments to verify", "Family record updates"); Quick Actions demoted from a full-width tile section to a compact bordered list occupying 40% of a row shared 60/40 with Today's Schedule, rather than removed outright, per the person's follow-up that they were fine keeping it at lower prominence; and a new **Diocese communication** section added alongside Recent Activity in a 2-column split, seeded with content matching what's already established on the Diocese Communication hub (Chrism Mass, revised subscription guidelines, clergy retreat schedule) for narrative consistency across the app. **Snapshot numbers updated** per explicit request (Families 1,842 → 5,236; Parishioners scaled proportionally using the same per-family ratio rather than picked arbitrarily — verified via calculation to be 17,396, catching and correcting my own initial mental-math rounding error of 17,397 before it shipped) — and because both `screen-parish-reports-hub.html` and `screen-diocese-reports-hub.html` independently hardcode this same parish's family count for their own cross-app consistency, both were updated in the same pass to avoid silently introducing a new contradiction between screens. Verified via script test: hero includes the parish name in its subtext as explicitly requested, all label/number text changes are present, the old sections are genuinely gone (not just hidden), and the empty state still renders correctly after `quickActionsSection()` was refactored to a shared component reused by both states. **Second polish pass, five specific fixes**: (1) the "Payments to verify" Action Center card had a dead `go: null` link — replaced with a "Facility bookings to confirm" card wired to a new `goFacilityBookings()` helper deep-linking to Requests hub's real Facility Bookings tab (2 genuinely pending bookings in that tab's own seed data, verified to match). (2) Hero banner given a soft gradient wash (primary-light → lavender → gold) instead of a flat white card, while staying at the same compact height from the earlier redesign — vibrance without reintroducing the bulk that was removed. (3) Quick Actions given a fifth item ("View Reports", wired to the real Reports hub) specifically to bring its height closer to Today's Schedule's, since a 4-item list was visibly shorter than the 3-item schedule (each schedule row carries two lines of text, quick action rows only one). (4) Action Center cards given a light tone-tinted background per card (a lighter tint than the icon's own badge tint, via a separate `cardBgMap`) so they read as a distinct zone from the plain white cards below. (5) Icons enlarged from 22px to 28px, and all five labels rewritten to name the pending action rather than just the category — "Sacraments awaiting approval" instead of "Sacrament approvals", which read as a completed state rather than a queue. Verified via script test: the new facility-bookings link resolves correctly and is a real onclick handler rather than a toast, the fifth quick action renders in the correct position with a working destination, and the empty state still renders all five quick actions correctly after the data change. **Contrast fix, caught from an actual rendered screenshot rather than a code review**: the light tints introduced in the tinted-card pass (bg-{color}/10) turned out to render nearly invisible against the page background (`bg-warm` #FAFBFD) — a screenshot of the live page showed the hero banner and Action Center cards visually blending into the page rather than standing apart, something a syntax check or DOM-content script test couldn't have caught since the actual pixel values weren't being verified, only presence of the right classes. Fixed by doubling the tint opacity (10 → 20, matching the saturation already used for icon badges elsewhere in the app) and adding a matching-color border (e.g. `border-lavender/25`) to every tinted card and the hero banner, giving each a crisp edge independent of the fill color's contrast against the page. The same fix was proactively applied to the Diocese Dashboard's snapshot cards, hero banner, and Parish Health summary cards, since they were built with the identical `/10` pattern and would have had the exact same problem the first time anyone looked at a real screenshot |
| `screen-parish-familyrecords-search.html` | Parish | Family Records | ✅ Done | Table + right slide-in drawer, sort/filter (status, ward, subscription) + cross-field search. Header shows Registered/Active family counts. Family members now individually editable (name/relationship/DOB/status) via a per-row Edit action. Subscription tab redesigned: prominent color-coded Total Outstanding hero, "Subscription paid till" label, Record Payment shown as its own bounded card with a live, prominent preview (not subtext), payment confirmation replaces the form in place rather than appearing near history. Wards corrected to Infant Jesus Shrine's real catchment (Viveknagar, Ejipura, Neelasandra, Austin Town, National Games Village), now a fixed dropdown everywhere a ward is set. Register Family asks for husband/wife names, marriage date, and marriage parish. **New "Updates" tab** in the drawer reviews parishioner-submitted Family Update Requests (Approve applies the diff directly to the record; Reject requires a message) — tab label shows a pending-count badge. Supports deep-linking via `?family=ID&tab=updates` (used by the Dashboard priority item) and `?openRegister=1` (used by the "Register New Family" quick action, now actually wired up). Subscription pills simplified to just "Paid"/"Pending"/"Overdue" (dropped the redundant "Subscription" prefix everywhere it appeared, including the filter dropdown). Family status "Temporarily Away" renamed to **"Away"**. **Drawer tabs restructured properly** (the earlier `flex-wrap` fix was a band-aid, not the real fix): removed **Transfer** as a standalone tab and moved it into a new **"Family Actions"** section at the bottom of Overview, grouped alongside two new actions — **Request Temporary Suspension** (sets status to Away, with an inline confirm) and **Archive Family Record** (sets status to Archived, with an inline confirm) — both genuinely functional against the existing `status` enum, not placeholders. This freed up the tab slot for **Updates**, so the drawer is now 4 tabs (Overview/Members/Subscription/Updates) instead of 5, fitting comfortably within the drawer's width without needing to wrap. Verified end-to-end via a sandboxed script test: tab set is correct, Family Actions section renders with all three CTAs, and suspend → resume → archive all correctly transition `status` and update the UI (CTAs correctly disappear once archived) |
| `screen-parish-masses-hub.html` | Parish | Mass Services | ✅ Rebuilt | **Corrected data model per the Mass Services Build Specification**: a Booking can contain multiple Intentions, each with its own Mass/date, matching MassActivity → MassInstance → MassBooking → MassIntention. **Manage Parish Masses** tab (renamed from "Mass Schedule"): masses now named with language (English/Tamil/Kannada/Konkani), booking caps raised to 20, three new Feast-type masses added (Assumption, Christmas Midnight, Easter Vigil, Good Friday), custom 5-minute-step time picker (three plain `<select>`s — hour/minute/AM-PM — so every option is visible without scrolling, replacing the native time input), an "Intention & booking settings" separator groups the fee/cap/cutoff fields, and every mass now has a working **Edit** button that reopens the same modal pre-filled and updates in place (verified via a sandboxed script test — no duplicate created). Grouped sections (dynamically derived by day-pattern/date-range, from the earlier round) carried forward. **Upcoming Masses**: badge now shows only the confirmed count, no "x of y"; seeded enough bookings that the first 8 chronological instances each show 3–5 real bookings — found and fixed a second sorting bug in the process (Mass times were compared as plain strings, so "6:00 PM" sorted before "6:45 AM"; fixed with a proper time-to-minutes comparator, verified against the real seed data before and after). **Bookings**: now a flat table (never grouped) showing Booked By (name primary, booking # demoted to secondary text below it) / Mass / Intention type+description / Source / Created By / a single combined **Status** column (Pending Payment / Confirmed / Confirmed (Waived) / Payment Failed — collapsing what were two separate Payment+Status columns into one); Stipend column removed from the table (still visible in the detail view); the Mass filter now lists actual date+time+name instances rather than just activity types, plus a free-text search box; CTA renamed **"New Booking"**; the modal itself was reimagined into a two-pane layout (date + Mass list on the left, intention details on the right) with a **cart** that holds multiple intentions and submits them as one booking with one payment — verified end-to-end in a script test (2 intentions added, exactly 1 booking created, cart cleared after). **New: Day Schedule tab (Parish-side only — deliberately not built on the Parishioner side)** — pick any date and see a merged, time-sorted view of that day's published recurring Masses (with live confirmed-booking counts) alongside unpublished one-off pastoral events (funerals, weddings, etc.), so staff can see real open slots before committing to a request. "Add Mass for This Day" creates these as a genuinely separate `adHocEvents` entity — no MassActivity record, no generated bookable instance, never surfaced to parishioners — clearly labeled "Not published" in the list. Verified via a sandboxed script test: entries merge and sort correctly by time, and a newly-added event appears in the right position immediately. **Latest round of fixes**: found and fixed two real bugs — `generateInstances()` was missing the `bookingFee` field entirely (root cause of "₹undefined" in the New Booking modal's mass list), and three icon() calls across the file (delete/status/confirmed-count icons) were missing the base `.icon` CSS class that provides stroke/fill styling, making them invisible or black-filled instead of clean colored line icons. **New Booking modal fully redesigned**: now a wide 3-panel layout (month calendar + Mass list on the left, intention form in the middle, cart + payment on the right), replacing the old date dropdown with an actual clickable calendar (days with scheduled Masses highlighted), verified via script test (bookingFee present on every instance, no "undefined" anywhere in the rendered modal, month navigation works). Booking Bookings tab renamed to **Mass Bookings**; "Add Office Booking" renamed to **"Add Mass Booking"**; booking detail moved from inline-below-table to its own **modal** for consistency; **Sources filter removed** from the Bookings toolbar (Source column itself kept); "In Memory Of" renamed to **"For the Soul Of"** across this file and both parishioner Mass Booking screens; Upcoming Masses pill renamed **"confirmed" → "bookings"**; Day Schedule entries for published Masses no longer show booking counts (shows Mass type instead); mass cards in Manage Parish Masses redesigned from stacked multi-line cards to **compact single-line rows** (name+badges, then time/fee/max on one muted line, status+edit on the right), cutting the scroll height significantly with many masses on screen. **Fixed the New Booking CTA wrapping onto its own line in the Bookings toolbar** — root cause was the Mass filter `<select>` sizing itself to its widest option (date+time+full mass name across ~30 instances), overflowing the flex row; capped its width and pinned the button with `flex-shrink-0`. **Day Schedule tab extracted out entirely** — see `screen-parish-calendar-hub.html` below; this file no longer has any day-schedule/ad-hoc-event code |
| `screen-parish-calendar-hub.html` | Parish | Parish Calendar | ✅ Built | **Promoted out of the Masses hub into its own top-level nav item** (sits between Masses and Services), specifically for discoverability — a day's full schedule (Masses plus one-off pastoral events) is useful to more roles than just whoever's managing Masses, and it was easy to miss as a buried tab. Pick any date and see a merged, time-sorted view of that day's published recurring Masses (with live confirmed-booking counts) alongside unpublished one-off events, so staff can see real open slots before committing to a request. **Multi-type, multi-venue**: `EVENT_TYPES` covers Funeral/Wedding/Prayer Service/Adoration/Other (not just Masses), `VENUES` covers Main Church/Parish Hall/Meeting Room A/Outdoor Grounds, and a venue filter dropdown narrows the day's view to just one location — verified via script test that filtering to "Parish Hall" correctly isolates a Parish-Hall-only event while excluding a Main-Church-only Mass on the same day. "Add Event for This Day" creates these as a genuinely separate `adHocEvents` entity — no MassActivity record, no generated bookable instance, never surfaced to parishioners — clearly labeled "Not published." Verified via script test: entries merge and sort correctly by time, Adoration and Prayer Service events render alongside Masses correctly, and venue filtering excludes non-matching entries as expected |
| `screen-parishioner-massbooking-request.html` | Parishioner | Book a Mass | ✅ Rebuilt | Date + Mass merged into one accordion step; cart collapsed by default with a "Review & Pay →" shortcut always available; intention description/help adapts per type; parish search replaced native `<datalist>` with a custom results list; sticky (not fixed) footer + `min-h-[100dvh]` so the button doesn't hide under the mobile keyboard. **Found and fixed the actual cause of the date-grouping bug**: `.toISOString()` converts to UTC, and since Bangalore is UTC+5:30, constructing a local midnight Date and slicing the ISO string rolled the calendar date back by one day for anyone in an ahead-of-UTC timezone — this is why Sunday Masses were grouping under a Saturday header. Replaced with a local-date formatter; same fix applied to `screen-parish-masses-hub.html`. All booking fees now minimum ₹100 (bumped the two Weekday Masses that were ₹50). 5-item bottom nav added, stacked below the Continue button (an explicit exception to the usual wizard-excludes-bottom-nav rule). "In Memory Of" intention type renamed to "For the Soul Of" |
| `screen-parishioner-massbooking-mybookings.html` | Parishioner | My Mass Bookings | ✅ Rebuilt | Now booking-centric, not intention-centric: each card is one Booking (Booking #, total, payment status) that expands to show every Intention inside it (Mass, date, type, description, offered by, stipend). Upcoming/Past classified by whether any intention's date is still ahead. Seeded with a single-intention Paid booking, a Pending-payment booking (working "Pay Now"), a past Parish-Office booking, and a genuine **multi-intention** booking spanning two different Masses, to demonstrate the corrected model. 5-item bottom nav (Services highlighted). "In Memory Of" intention type renamed to "For the Soul Of" |
| `screen-parishioner-services-hub.html` | Parishioner | Services (hub) | ✅ Built | New landing page for all four service categories (Documents & Certificates, Sacraments, Home Services, Facility Booking), each a tappable card linking to its request flow, plus a "Your activity" section linking to all four "My X" list screens. Closes the long-standing gap where the bottom nav's "Services" tab was a `toast()` placeholder everywhere — now wired up across all 6 existing Parishioner screens that have a bottom nav (Dashboard, Family, Documents My Requests/My Documents, Mass Booking request/My Bookings), including making the "active but inert" Services tab icon on 4 of those screens actually clickable |
| `screen-parishioner-sacraments-register.html` | Parishioner | Sacraments — Register | ✅ Built | 5-step guided flow (Sacrament → Candidate → Documents → Review → Payment), mirroring the wizard pattern established for Mass Booking and Document Requests. Sacraments mirrored from the Parish-side Services config (Baptism/First Communion/Confirmation/Marriage — name, registration window, min age, parish membership required, required documents, fee), now shown inline on each sacrament option card so eligibility is visible before selecting, not just buried in a doc list later. Document upload step is a tap-to-simulate toggle per required document, and the Continue button is genuinely gated — validated via script test that it blocks progression until every required document is marked uploaded, then allows it once they are. **Baptism handled specially, since a newborn isn't in the family record yet**: the "candidate" step becomes a baby-details form instead of a family-member picker — name (optional, falls back to "Baby of {parent}" if left blank, to be updated once the baby is formally added to the family), date of birth, parent, and a **requested-date dropdown computed from the parish's actual recurring schedule** (`nextScheduledDates()`, generates the next 4 "first Sunday of the month" dates from a `scheduleRule` field on the sacrament config) rather than a free-form date picker. For the other sacraments, the family-member picker now shows each member's age and a clear eligibility flag if they're below the sacrament's minimum age. Review step surfaces the full eligibility summary (min age, membership requirement) alongside the existing date/venue/document info. Payment step: UPI/Card/Net Banking, matching the convention used elsewhere. All of this verified via a script test: correct recurring dates generated, correct age computed from DOB, candidate-step validation correctly blocked and then passed, and the "Baby of X" fallback label confirmed correct with and without a name entered |
| `screen-parishioner-sacraments-myregistrations.html` | Parishioner | Sacraments — My Registrations | ✅ Built | List of the family's sacrament registrations — status badge (Submitted/Approved/Rejected), payment status, preparation progress note, celebration date where applicable. Seeded with the existing Clara Rodrigues Confirmation (Approved, from `mock-seed.json`) plus a new Submitted example to show the pending state |
| `screen-parishioner-homeservices-request.html` | Parishioner | Home Services — Request | ✅ Built | Single unified 3-step flow (Service → Details → Review) covering all Active home service types from the Parish-side config, rather than a separate screen per type — matches "Simple by Default." Services flagged `emergencyAllowed` (Sick Visit/Anointing, Funeral Visit/Prayer) show an "emergency — please prioritise" checkbox that, when checked, skips the date/time fields entirely (the request is handled as soon as possible instead). No payment step — home services are free, matching the schema |
| `screen-parishioner-homeservices-myrequests.html` | Parishioner | Home Services — My Requests | ✅ Built | List of home service requests with status (Pending Approval/Scheduled/Completed) and an emergency flag shown inline where applicable. Seeded with the existing House Blessing request from `mock-seed.json` plus an emergency Sick Visit example |
| `screen-parishioner-facilities-request.html` | Parishioner | Facility Booking — Request | ✅ Built | 4-step guided flow (Facility → Details → Review → Payment). Facilities mirrored from the Parish-side Services config (Parish Hall/Meeting Room A/Outdoor Grounds — capacity, booking fee, availability). **Updated to match the Parish-side Facilities rework** (which replaced free-text availability with structured `availableDays` + `slots`): facility cards now show which days each space is available and its named time slots (e.g. Morning/Evening) instead of a free-text string; the Details step replaced free-form start/end time inputs with a **slot picker** (selectable cards, one per configured session) and validates the chosen date against the facility's actual `availableDays` — picking a Sunday for Meeting Room A (Mon–Sat only) is blocked with a clear message naming the day and facility, while Outdoor Grounds (Sat/Sun only) correctly accepts weekend dates and rejects weekdays. Review and confirmation both show the selected slot's name and time range rather than raw times. Verified via script test: day-of-week computed correctly from ISO dates, validation correctly blocks and then passes for two different facilities with different day rules, and the full flow (facility → details → review → payment → confirmation) completes with the right slot displayed at each step |
| `screen-parishioner-facilities-mybookings.html` | Parishioner | Facility Booking — My Bookings | ✅ Built | List of facility bookings with date/time, purpose, payment status, and booking status |
| `screen-parishioner-donate-hub.html` | Parishioner | Donate (hub) | ✅ Built | **Built following a full Parishioner-side audit** that found this entire spec section (Donate) completely unbuilt — "Give" in the bottom nav had toasted since the very first Parishioner screens were built. Two tabs. **Give**: the same 2 real causes already referenced (but not actionable) on the Dashboard's Community section (Church Renovation Fund — goal-based with a progress bar, Education Support Fund — open), each with a bottom-sheet guided flow (suggested amounts or custom, payment method, confirm) that actually updates the cause's raised total and logs a real donation. **History**: past donations with receipt numbers (simulated download, consistent with how every other document/receipt download works across this whole app). Verified via script test: amount and payment-method validation both block submission correctly, both suggested and custom amounts work, the cause's progress bar total updates after giving, and the new donation appears correctly in History |
| `screen-parishioner-me-hub.html` | Parishioner | Me (hub) | ✅ Built | **Also built from the audit** — "Menu" had toasted the same way since the beginning, and three of this section's five spec items (Membership & Subscription, Activity, Settings, Help — Family already existed as its own page) didn't exist anywhere. Family stays a top-level quick-link card here rather than a tab, since it already has its own bottom-nav entry and dedicated page — no reason to duplicate it. **Membership & Subscription**: real Q3 (Paid, from `mock-seed.json`'s actual `subscriptionLedger`) plus a new Q4 (Pending, due 1 Oct 2026) added specifically to make the Dashboard's existing "Subscription payment reminder" card mean something — a real Pay Now flow that clears the outstanding balance and flips status to Paid. **Activity**: a unified chronological feed spanning every request/booking/payment type in this app (documents, home services, sacraments, donations, subscription) rather than a single-module list. **Settings**: account info (read display) plus notification channel toggles. **Help**: an FAQ accordion plus real parish office contact details (matching Infant Jesus Shrine's actual seeded phone/address/hours). Verified via script test: the subscription payment flow is blocked without a method and correctly clears the balance once confirmed, notification toggles flip state, and the FAQ accordion opens/closes correctly |
| `screen-parishioner-family.html` | Parishioner | Me → My Family | ✅ Done | Read-only family overview + member list, plus a simple (non-wizard) "Request an Update" form covering the three most common cases: Update Family Details, Update a Member's Details, Add a Family Member — each pre-fills known values per the Capture Once principle. Submissions appear in "My Update Requests" with status tracking (Submitted/Approved/Rejected + reason if rejected). Linked from the Dashboard's "My Family" bottom-nav item. 5-item bottom nav added (My Family highlighted). Scoped deliberately: Remove Member, Record Birth/Marriage/Death, and Parish Transfer are out of this pass (Transfer already exists staff-initiated in Family Records) |

| `screen-diocese-dashboard-home.html` | Diocese | Dashboard (home) | ✅ Done (Community Heart) | Sidebar nav; follows the locked Diocese Dashboard spec (Snapshot → Action Center → Parish Health → Upcoming → Announcements); Parish Health uses real parishes from the directory. **Brought up to the navigation convention used everywhere on the Parish side** — this file predated `sessionParams`/`baseParams()`/`goX()` (it was one of the very first three anchor screens built, before that convention existed), so it had none of it; added now so "Parishes", "View all parishes", and route to the hub generally rather than deep-linking to a specific parish's detail, since this dashboard's `parishHealth` seed data only has name/locality/reason — no parish ID to match against the directory cleanly; noted as a deliberate simplification rather than a silently-dropped feature. "Reports" sidebar link also wired up to the new Reports hub |
| `screen-diocese-parishes-hub.html` | Diocese | Parishes | ✅ Done | **First Diocese-side module built**, per an explicitly agreed build order. All **130 real parishes from `parishes.json`** embedded directly. **Restructured per direct feedback** after the original 3-tab version (Directory/Parish Health/Parish Support): tabs removed entirely — Directory and Parish Health merged into one unified, filterable table (they were describing the same set of parishes anyway), and Parish Support (Onboarding/Module Enablement/Adoption) dropped outright as not required. **Table columns changed**: est. families column removed ("just size of a parish is enough" — size tier alone remains), and Parish office phone + Parish Priest name + priest phone added as real columns, generated deterministically per parish (office numbers as 8-digit Bangalore-landline-style, priest numbers as 10-digit mobile-style) since no phone data exists in the source dataset — Infant Jesus Shrine keeps its one genuine seeded office number (25301206) rather than a generated one. **New region filter**, derived from real pincode prefixes embedded in each address rather than invented — classified into Bengaluru City (560, 87 parishes) / Bengaluru Rural (561–562, 20) / Kolar & Chikkaballapura (563–564, 16) / Tumkuru (571–572, 7), verified against the actual source data with zero unclassified addresses before it shipped. **Health rebuilt as three independent pillars — Financial, Community, Governance — shown as three separate red/amber/green dots in one column**, replacing the old single Healthy/At Risk/Not Onboarded status. Critically, **health no longer depends on onboarding stage or enabled modules at all** (the person's explicit ask, after a design conversation about what should actually feed a bishop-facing health score) — each pillar has its own seeded metric with a real number behind it, not a canned reason: Financial = subscription collection rate (threshold ≥70 Green / ≥50 Amber / below Red), Community = registered-family year-over-year growth rate (≥0 Green / ≥-5 Amber / below Red, can go negative), Governance = monthly reports submitted on time out of the last 6 (≥5 Green / ≥3 Amber / below Red) — verified via script test that pillars are genuinely independent (found real parishes with mixed Green/Amber/Red across their three pillars, not one score copied three times) and that the flagship parish's financial metric (82%) matches its subscription collection rate shown elsewhere in the app. `modulesFor()` and `onboardingStageFor()` removed from the file entirely as dead code rather than left unused. **Ripple effects checked and fixed**: the Diocese Dashboard's "New parish onboarding" Action Center tile linked to the now-deleted Parish Support tab via a `goParishSupport()` helper — caught this before it could ship as a dangling link, repointed to the general Parishes hub and removed the dead function. Detail modal on row click now shows region, both phone numbers, and the full three-pillar breakdown with each metric's actual number, not just a color. Verified via script test throughout: region classification, phone number formats and determinism, health independence, and the modal's field-by-field content |
| `screen-diocese-reports-hub.html` | Diocese | Reports | ✅ Done | **Second module in the agreed build order.** Seven tabs matching the Nav Hierarchy spec exactly. All 130 real parishes re-embedded (same dataset as the Parishes hub — each Diocese file keeps its own consistent copy, matching the "Option A" convention used throughout the Parish side, since there's no shared component system). **Extended the deterministic per-parish model** from the Parishes hub with a `metricsFor(parish)` function generating requests processed, families registered, all four sacrament counts, income, subscription rate, and Faith Formation content volume — seeded from each parish's own ID (not `Math.random()`) and **weighted by size tier** (Large ×3, Medium ×1.6, Small ×1), so bigger parishes genuinely show proportionally bigger numbers rather than random noise; verified via script test that Large-parish averages come out ~3× Small-parish averages, not by coincidence. **Infant Jesus Shrine's numbers are hardcoded to match its real seeded data exactly** (42 Baptisms, ₹18,42,600 income) rather than running through the synthetic generator — verified via script test against the actual figures established on the Parish side, so the diocese-level rollup and the parish-level detail agree with each other rather than silently diverging. **Parish Activity**: diocese-wide request/family totals, most/least active parishes (ranked lists), 6-month volume trend. **Sacraments**: YTD totals for all four types, average Baptisms by size tier, monthly trend. **Finance**: total income, average collection rate, income by size tier, a "lowest collecting" list (the kind of thing a diocese finance office would actually want flagged). **Events**: Diocese-organized vs. Parish-organized split, an upcoming-events list, attendance ranking for the largest events. **Faith Formation**: adoption rate (parishes publishing vs. not), content volume by category (matching the same five categories built on the Parish side), a sample list of non-publishing parishes. **Parish Comparison**: the one genuinely interactive tab rather than a fixed report — three parish-picker dropdowns (2 pre-selected by default, third optional) driving a live side-by-side comparison table across 7 metrics; verified via script test that changing a selection updates the table, adding a third parish works, and clearing all selections shows a proper empty state rather than an error. **Export**: same pattern as the Parish-side Export tab (report type + date range + format + working recent-exports log). Sidebar "Reports" link wired up on both the Dashboard and Parishes hub (previously toast placeholders). Verified via script test: all seven tabs render substantial real content, and the full export flow correctly adds a new entry |
| `screen-diocese-communication-hub.html` | Diocese | Communication | ✅ Done | **Third module in the agreed build order** — the smallest lift, per the plan, since Announcements and Circulars mirror patterns already proven on the Parish side almost exactly (title suggestions, status-by-action, inline attachments, search-by-text/date for Announcements; title/category/attachment/status for Circulars) rather than reinventing them. **Announcements**: full parity with the Parish-side rework (Save as Draft / Publish buttons, attachment chips shown inline rather than requiring download, live search). **Circulars**: same pragmatic flat-table model as the Parish side, diocese-relevant categories (Liturgical/Clergy/Finance/Administrative/Pastoral). **Broadcast Notifications** (genuinely new — nothing on the Parish side needed this, since a parish only ever talks to its own parishioners): compose a message, pick a recipient scope — All Parishes, by Size Tier, or Specific Parishes via a multi-select built from a lightweight 130-parish list (id/name/sizeTier only, not the full directory record, to keep this file leaner than Parishes/Reports which need the full detail) — then pick one or more channels (In-App/Email/SMS/WhatsApp), and send; a Sent Broadcasts log shows scope and channels for each. Verified via script test: message and channel validation both correctly block sending, the "Specific Parishes" scope is blocked until at least one parish is selected, and all three scope types produce the correct summary label (confirmed "By Size Tier" genuinely filters against the real 130-parish distribution — Large parishes came back as exactly 25, matching the actual count in the directory). **Diocese Templates**: covers the Template Hierarchy's Diocese tier (templates owned and published by the diocese, inherited by parishes per `DOC-ISD-003`) — unlike the Parish-side Documents hub, which only *views* templates, this one manages their name/category/status (Draft → Published → Archived), since the diocese is the actual owner here. Template layout/placeholder design is explicitly out of scope, same reasoning as the Parish side — noted directly in the UI copy, not just in this log. Sidebar "Communication" link wired up on the Dashboard, Parishes hub, and Reports hub (previously toast placeholders) |
| `screen-diocese-ministry-hub.html` | Diocese | Ministry | ✅ Done | **Fourth module in the agreed build order.** Three tabs. **Diocese Events**: reuses the exact three diocese-organized events already seeded in Reports' Events tab (Confirmation Mass, Annual Priests' Retreat, Diocesan Youth Convention) as this hub's editable source of truth, plus two more for variety — so the Reports rollup and this hub's event list agree with each other rather than describing two different sets of events under the same names. Full CRUD (status-by-action, matching the convention used everywhere else) plus a Registrations modal with tap-to-toggle attendance, mirroring the Parish-side Communication Events pattern closely since it already works well and there was no reason to redesign it. **Faith Formation**: a diocese-published content library that becomes available to every parish's own Faith Formation library once published — deliberately distinct from Reports' Faith Formation tab, which shows *adoption* (how much parishes are actually using), while this is the *content itself*; same five categories as the Parish-side Faith Formation hub for continuity, with search and status-by-action. **Pastoral Ministries** (Family/Youth/Catechism/Vocations) was flagged upfront as having no direct Parish-side pattern to mirror — built as stat cards (total programs, total participants, avg. per program) plus a ministry-area tab selector, each showing named programs with a coordinator and participant count; verified via script test that switching tabs actually swaps content rather than just relabeling (Youth tab correctly excludes Family programs and vice versa). Sidebar "Ministry" link wired up on all four other Diocese screens (previously toast placeholders). Verified via script test: all three tabs render substantial content, Diocese Events CRUD/registrations/attendance-toggle all work correctly, Faith Formation search narrows results and status-by-action publishes correctly, and Pastoral Ministries' tab switching genuinely changes the displayed program list |
| `screen-diocese-settings-hub.html` | Diocese | Settings | ✅ Done | **Fifth and final module in the agreed build order — completes the entire Diocese side.** Four tabs. **Parish Management**: deliberately distinct from the Parishes hub's Directory (which is oversight/health-focused, read-mostly) — this is the administrative CRUD surface, with a searchable 130-parish table (capped at 25 rows shown with a "refine your search" hint, since rendering all 130 in the same kebab-menu-plus-fixed-position pattern isn't necessary for a demo), Register New Parish (name/address/size tier/priest), and Deactivate/Reactivate per parish via the same kebab-menu-plus-inline-confirm pattern established on the Parish side — including the fixed-position-appended-to-body technique to avoid the table's `overflow-hidden` clipping the dropdown, the same real bug that was caught and fixed once already this build. **Users & Roles**: diocese office staff (Diocese Administrator, Vicar General, Communications Officer, Finance Officer, Pastoral Coordinator) with Invite/Edit/Disable via the identical kebab pattern — two independent kebab systems (parish actions and user actions) coexist on this page without interfering, verified via script test using separate DOM tracking for each. **Global Configuration**: general settings (timezone/currency/date format/fiscal year) plus diocese-specific defaults that cascade to new parishes (suggested minimum subscription, template-approval requirement, auto-enabled modules for onboarding) — this second section is what makes it meaningfully different from a copy of the Parish-side System Configuration tab rather than a duplicate. **Notification Settings**: same channel-plus-event-matrix pattern as the Parish side, scoped to diocese-level events (Parish Onboarding Complete, Parish Health Alert, Broadcast Sent Confirmation, New User Invited) rather than parishioner-facing ones. Sidebar "Settings" link wired up on all five other Diocese screens (previously toast placeholders) — **this completes the Diocese sidebar**: every item across all six screens is now a real, built page with an identical, verified sidebar order. Verified via script test: all four tabs render, the parish kebab/confirm/deactivate flow and the user kebab/confirm/disable flow both work correctly and independently, registering a new parish correctly adds it to the top of the list, and both notification channel and event-level toggles flip their underlying state correctly |

---

## Mock-Data Foundation

| File | Purpose |
|---|---|
| `mock-data/mock-data-schema.md` | Canonical entity/field/status schema for every module, derived from the feature design briefs |
| `mock-data/mock-seed.json` | Instantiated seed data for the Rodrigues family + one sample record per module |
| `mock-data/parishes.json` | Real 130-parish Bangalore Archdiocese directory |

## Screen Specs (beyond the module briefs)

| File | Purpose |
|---|---|
| `specs/mass-services-build-spec.md` | User-provided Mass Services Build Specification — corrected data model (Booking → multiple Intentions, each with its own Mass/date), booking states (ONLINE: Draft→Pending Payment→Paid/Confirmed; PARISH_OFFICE: Draft→Payment Confirmed→Confirmed), and the parish/parishioner use cases that `screen-parish-masses-hub.html` and the two `screen-parishioner-massbooking-*.html` screens are now built against |
| `specs/diocese-dashboard-spec.md` | Diocese Dashboard section hierarchy (Snapshot / Action Center / Parish Health / Upcoming / Announcements) |

## Hosting & Linking Conventions

This entire set is built to be deployed to a static web host (Netlify/Vercel/GitHub Pages/etc.) as one flat folder, so it can be demoed on a phone (Parishioner flows) and a laptop (Parish/Diocese flows) from the same hosted URL.

- **`index.html`** is the demo launcher — links to each interface's home dashboard. Open the hosted root URL to get there.
- All screens are flat files (no subfolders) so relative links (`href="screen-parish-dashboard-home.html"`) resolve correctly regardless of host.
- **Build discipline**: a nav item links to a real file (`<a href="...">`) only once that target screen actually exists. Until then it calls `toast('Label')` as a placeholder. When a target screen is built, go back and swap any `toast(...)` calls that were meant to reach it into real `<a href>`/`onclick=location.href=...` links. This avoids broken links on a live hosted demo while still letting us build incrementally.

### Top-level Screen Map (locked filenames)

| Interface | Nav item | Filename | Status |
|---|---|---|---|
| Parishioner | Login | `screen-parishioner-login.html` | ✅ Built |
| Parishioner | Dashboard (home) | `screen-parishioner-dashboard-home.html` | ✅ Built |
| Parishioner | Services (hub) | `screen-parishioner-services-hub.html` | ✅ Built |
| Parishioner | Sacraments — Register | `screen-parishioner-sacraments-register.html` | ✅ Built |
| Parishioner | Sacraments — My Registrations | `screen-parishioner-sacraments-myregistrations.html` | ✅ Built |
| Parishioner | Home Services — Request | `screen-parishioner-homeservices-request.html` | ✅ Built |
| Parishioner | Home Services — My Requests | `screen-parishioner-homeservices-myrequests.html` | ✅ Built |
| Parishioner | Facility Booking — Request | `screen-parishioner-facilities-request.html` | ✅ Built |
| Parishioner | Facility Booking — My Bookings | `screen-parishioner-facilities-mybookings.html` | ✅ Built |
| Parishioner | Donate (hub) | `screen-parishioner-donate-hub.html` | ✅ Built |
| Parishioner | Faith Formation | `screen-parishioner-faithformation.html` | ✅ Built |
| Parishioner | Parish Events | `screen-parishioner-events.html` | ✅ Built |
| Parishioner | Committees & Ministries | `screen-parishioner-committees.html` | ✅ Built |
| Parishioner | Parish Profile | `screen-parishioner-parish-profile.html` | ✅ Built |
| Parishioner | Me (hub) | `screen-parishioner-me-hub.html` | ✅ Built |
| Parish | Login | `screen-parish-login.html` | ✅ Built |
| Parish | Dashboard (home) | `screen-parish-dashboard-home.html` | ✅ Built |
| Parish | Family Records | `screen-parish-familyrecords-search.html` | ✅ Built |
| Parish | Committees | `screen-parish-committees-hub.html` | ✅ Built |
| Parish | Requests (hub) | `screen-parish-requests-hub.html` | ✅ Built |
| Parish | Documents | `screen-parish-documents-hub.html` | ✅ Built |
| Parish | Masses | `screen-parish-masses-hub.html` | ✅ Built |
| Parish | Parish Calendar | `screen-parish-calendar-hub.html` | ✅ Built |
| Parish | Communication | `screen-parish-communication-hub.html` | ✅ Built |
| Parish | Faith Formation | `screen-parish-faithformation-hub.html` | ✅ Built |
| Parish | Finance | `screen-parish-finance-hub.html` | ✅ Built |
| Parish | Administration | `screen-parish-administration-hub.html` | ✅ Built |
| Parish | Services | `screen-parish-services-hub.html` | ✅ Built (moved under Administration, not primary nav) |
| Parish | Reports | `screen-parish-reports-hub.html` | ✅ Built |
| Parish | Settings | `screen-parish-settings-hub.html` | ✅ Built |
| Diocese | Login | `screen-diocese-login.html` | ✅ Built |
| Diocese | Dashboard (home) | `screen-diocese-dashboard-home.html` | ✅ Built |
| Diocese | Parishes | `screen-diocese-parishes-hub.html` | ✅ Built |
| Diocese | Ministry | `screen-diocese-ministry-hub.html` | ✅ Built |
| Diocese | Communication | `screen-diocese-communication-hub.html` | ✅ Built |
| Diocese | Documents | `screen-diocese-documents-hub.html` | ✅ Built |
| Diocese | Reports | `screen-diocese-reports-hub.html` | ✅ Built |
| Diocese | Settings | `screen-diocese-settings-hub.html` | ✅ Built |

Deeper flow-specific screens (e.g. within Requests → Documents & Certificates) get added as rows here once we build that stage, following the same `screen-[interface]-[flow]-[name].html` pattern.

### Bottom Nav Convention (Parishioner)

The 5-item bottom nav (Home/Services/Give/My Family/Menu) is present on every Parishioner screen **except**:
- `screen-parishioner-login.html` — pre-authentication, no nav context yet
- `screen-parishioner-documents-request.html` — guided wizard flow with a sticky footer Back/Continue bar

`screen-parishioner-massbooking-request.html` now includes **both**, stacked (Continue button on top, 5-item nav below) — added per explicit request despite the earlier general exception for wizards.

New Parishioner screens going forward should include the bottom nav unless they're a similar full-screen guided flow, in which case follow the same footer-bar pattern (or the stacked pattern, if both are wanted) instead.

### Auth / Session Convention

- No real backend — login screens accept any non-empty credentials (Parish login additionally validates the parish name against the real directory).
- Session context (role, name, parish id/name) is passed forward via **URL query parameters** on redirect (e.g. `screen-parish-dashboard-home.html?role=staff&parish=PAR-0002&parishName=...`), not `localStorage`/`sessionStorage` — keeps every screen a plain static file with no browser-storage dependency, works identically whether opened as a file, hosted, or viewed as a Claude artifact.
- Every post-login screen must read these params and reflect them in its header (name/role/parish), with a way to log out back to the correct login screen.
- **Parish data isolation demo**: the Parish login's parish picker accepts any of the 130 real parishes. Logging in as **Infant Jesus Shrine (`PAR-0002`)** shows the fully populated dashboard/data. Logging in as any other parish should render the genuine **Empty** state everywhere (no families, no requests) — proving parishes never see each other's data, without needing a second fully-seeded dataset.
- **Fixed a real cross-page navigation bug** (found across all 7 Parish hub screens — Dashboard, Family Records, Committees, Requests, Masses, Finance, Administration): every `go*()` sidebar navigation function built its target URL by blindly forwarding `sessionParams.toString()` — the *entire* current query string, including page-specific deep-link params like `family`, `tab`, `module`, `openRegister`. Once a user followed any deep link (e.g. Dashboard's "Family updates" tile → Requests hub → Family Records with `?family=FAM-1001&tab=updates`), those params got silently carried forward through every subsequent sidebar click, so clicking "Family Records" from anywhere else in the app would reopen that same family's Updates tab instead of showing the plain list. Fixed by adding a `baseParams()` helper to each file that keeps only the persistent identity params (`parish`, `parishName`, `name`) and using it everywhere a navigation URL is built — deep links that intentionally add their own params (e.g. `&module=familyupdates`, `&family=ID&tab=updates`) still work exactly as before, but nothing leaks forward unintentionally. Verified via a script test simulating the exact reported scenario (arriving via a deep link, then clicking a different sidebar item) before and after the fix.
- **Canonical Parish sidebar structure (locked) — now primary/secondary, not flat**: designed collaboratively across several turns (weighing usage frequency, not just "does this feel administrative") before any code was touched. **Primary** (flat, always visible): Dashboard → Family Records → Masses → Parish Calendar → Requests → Documents → Communication → Faith Formation → Finance → Reports → Settings. **Secondary** (nested under one expandable "Administration" parent, which is a pure expand/collapse toggle with no page or click-destination of its own): Parish Profile → Services → Inventory → Staff → Committees. This supersedes the earlier "Services lives in Administration's tab row" fix — Services (and now Committees) are proper sidebar-level secondary items, not tab-row links only discoverable after already landing on Administration. **Active-state behavior**: on the 11 "plain" screens the Administration group renders collapsed by default with a working expand/collapse toggle (`toggleAdminGroup()`); on the three screens that are themselves part of the group (`screen-parish-administration-hub.html`, `screen-parish-services-hub.html`, `screen-parish-committees-hub.html`) it renders pre-expanded with the correct child highlighted. On the Administration hub specifically, the sidebar's Parish Profile/Inventory/Staff highlighting is wired directly into the existing `setModule()` tab-switching function, so switching tabs *or* arriving via a `?module=inventory`/`?module=staff` deep-link both keep the sidebar and the tab row in sync — verified via script test that deep-linking to Staff correctly shows Staff active and Profile inactive in the sidebar, not just in the tab row. The now-redundant "Services" tab-row link inside Administration was removed (Services has a proper sidebar entry now), and the Services page's "Administration / Services" breadcrumb was repointed from the now-nonfunctional `goAdministration()` to `goAdminProfile()`, since Administration itself has no landing page to breadcrumb back to. Confirmed identical structure across all 14 Parish screens via script. Any new Parish screen must match this structure exactly.
- **Canonical Diocese sidebar order (locked, flat — no primary/secondary split needed at only 6 items)**: Dashboard → Parishes → Ministry → Communication → Reports → Settings, matching the Nav Hierarchy spec exactly. **The full Diocese side is now built out** — every item is a real page, no placeholders remain. Confirmed identical across all 6 Diocese screens via script. The Diocese Dashboard predated the `sessionParams`/`baseParams()`/`goX()` navigation convention entirely (it was one of the first three screens built, before that convention existed) and was retrofitted with it partway through this build-out so it could link to the rest of the Diocese side. **Shared data conventions across the Diocese side**: all 130 real parishes from `parishes.json` are re-embedded independently in Parishes, Reports, Communication, and Settings — each file keeps its own copy sized to what it actually needs (full detail in Parishes/Reports, id+name+sizeTier in Communication's broadcast picker, id+name+sizeTier+priest in Settings' Parish Management), rather than one bloated shared payload; real operational data only exists for Infant Jesus Shrine (PAR-0002) everywhere in this app, so every other parish's numbers across Parishes/Reports/Ministry are derived **deterministically from that parish's own ID** (never `Math.random()`), verified repeatedly via script test to give identical results on repeat queries and to correctly reflect real distributions (e.g. exactly 25 Large parishes, matching the source data) 
rather than being arbitrary noise.
- **Diocese-side audit (post-build)**: went through all 6 screens the same way the Parish side was audited — hunting for `toast()` calls that represent missing *features* rather than simulated micro-actions, and cross-checking against the Nav Hierarchy spec. Found and fixed three real issues. **(1)** Two Dashboard Action Center tiles ("New parish onboarding," "Diocese announcements awaiting approval") and the "View all" announcements link were still `toast()` placeholders despite the Parishes and Communication hubs now existing with exact matching content — wired up via a new `goParishSupport()` helper (deep-links to Parishes hub's Support tab) and the existing `goCommunication()`; also added `?module=` deep-link support to the Parishes hub itself, which hadn't had it. **(2)** Two other tiles ("Parish reports pending," "Parish profile approvals") were deliberately left as `toast()` — no screen anywhere actually represents a monthly-report-submission tracker or a profile-approval workflow, so linking them to the nearest approximation would have been worse than an honest placeholder, same reasoning applied to the Parish-side Payments tile earlier. **(3)** A genuine regression, not just a missing feature: all 5 hub screens built during this session (Parishes/Reports/Communication/Ministry/Settings) shipped with a non-interactive avatar badge — no account menu, no logout — while the original Dashboard has always had both. Fixed by adding the identical account-menu-with-logout block to all 5. Caught and corrected a second, more subtle bug in the same fix: the first pass placed the dropdown as a sibling *after* `</header>` instead of nested *inside* it like the Dashboard's original — since the header is `position: sticky` (which establishes a containing block for absolutely-positioned descendants), moving the dropdown outside it would have broken its position-relative-to-the-header behavior on scroll, a real rendering bug that wouldn't have been obvious from a syntax check alone. Verified the corrected placement matches the Dashboard's structure exactly and confirmed header tags remained balanced (one open, one close) across all 5 files after the fix. **Diocese Dashboard polish pass, mirroring the Parish Dashboard fixes**: (1) two Action Center items had `go: null` dead links ("Parish reports pending," "Parish profile approvals") — same audit finding as before, fixed by renaming to match real destinations rather than inventing new pages: "Parish activity reports" now links to Reports hub, "Parish registration requests" now links to Settings' Parish Management tab (where registering/deactivating parishes actually happens). (2) Diocese Snapshot's 4 stat cards given tone-varied light background tints (primary/green/lavender/gold), same `cardBgMap` pattern as the Parish Dashboard. (3) This dashboard had no hero banner at all before this pass — added one with the same gradient wash as the Parish Dashboard's redesign, plus a greeting, since a bare "Diocese snapshot" heading with no card wrapper read as unfinished. (4) This dashboard also had no Quick Actions section at all — added one (5 items: Register New Parish, Send Broadcast, Add Diocese Event, View Reports, Parish Directory, all with real destinations) paired 60/40 with the existing Upcoming section, matching the Parish Dashboard's layout. (5) **The most involved change**: replaced the old "top 5 parishes needing attention" list (hardcoded names, not connected to any real computation) with a genuine two-number summary — Good health / Requires attention — computed by re-embedding the exact same three-pillar health model from `screen-diocese-parishes-hub.html`. This surfaced a real modeling problem before it shipped: the original per-pillar thresholds, being three independent ~40-50%-Green dice rolls, made "all three Green" occur in only 11 of 130 parishes (8%) — mathematically correct but would have looked like a demo bug (91% of parishes "needing attention"). Fixed by redesigning the health model itself to use a shared per-parish "wellness" base with independent noise per pillar — more realistic (a genuinely struggling parish tends to show it across more than one dimension, not have anti-correlated pillars) and it also resolves the compounding-probability problem — then defining "good health" as "no Red pillar" rather than "all Green," landing on a believable 65/65 (50/50) split. Since this required changing the underlying model, **the Parishes hub was updated with the identical formulas** to keep the two files consistent rather than let them silently diverge — verified via script test that both files compute exactly 65 good / 65 attention from the same 130-parish set, and that the flagship parish, determinism, and genuinely mixed per-parish pillar statuses (confirmed earlier for the Parishes hub) all still hold after the model swap. **Table decluttered per direct feedback**: health legend removed from the filter bar entirely — it duplicated information already available via hover, since each health dot already carried a `title` attribute showing its pillar and status (confirmed this was already true rather than assuming, before removing the legend as redundant). Region column removed from the table body ("not required upfront") but the region filter dropdown itself stays functional, and region is still shown on drill-down in the detail modal — the data isn't gone, just not competing for space in every row when it's rarely the first thing someone scans for. Priest phone number merged into the Parish priest cell, shown as a smaller second line under the name (same pattern already used for parish name plus locality), rather than its own column — cutting the table from 7 columns to 5. Verified via script test: legend gone, region filter still narrows results correctly, merged priest cell shows name and phone in the right order, hover tooltips still fire on all three health dots, and the modal still surfaces region for anyone who wants it |
| `screen-diocese-documents-hub.html` | Diocese | Documents | ✅ Done | **New sixth Diocese module**, requested directly rather than part of the original agreed build order — a dedicated space to create standard circulars and diocesan documents from templates and share them with parishes, distinct from Communication hub's "Diocese Templates" tab (which only manages template status) and from Broadcast Notifications (which sends short messages, not formal documents). Three tabs. **Templates**: the same 4 diocese templates already defined in Communication hub (Diocesan Circular, Bishop's Pastoral Letter, Parish Visitation Report, Diocesan Event Invitation), re-embedded here per Option A convention since this is where they're actually *used* rather than just managed — each now carries its own guided-field schema (4-6 fields per template, tailored to what that document type actually needs: a circular wants a reference number and message body, a visitation report wants observations and recommendations, an event invitation wants a venue and RSVP line). **Create Document**: applies the layout lessons from the Parish-side Documents hub redesign from the start rather than needing a second pass — 40/60 split (guided fields : preview) and a fixed 560px-height preview with internal scroll, both proactively carried over rather than rebuilt the same way twice. The preview itself is a formal diocesan letterhead (a circular seal-and-cross crest, "Bangalore Archdiocese" in Playfair Display, a reference number and date in the header, template-specific body layout — the visitation report renders as a structured field grid, the event invitation as formal invitation prose, the circular and pastoral letter as a signed letter body) rather than one generic template renderer, so each document type actually looks like what it is. **Generate & Share** replaces the Parish-side flow's Print/Email actions with a share-scope picker — All Parishes / By Size Tier / Specific Parishes — reusing the exact same pattern and parish dataset already established for Communication hub's Broadcast Notifications, rather than inventing a new recipient-picker UI. **Shared Documents**: a log of what's gone out, to whom, and under what reference number. Sidebar "Documents" link added to all 6 other Diocese screens (positioned between Communication and Reports) plus this one — confirmed identical sidebar order across all 7 Diocese screens via script, matching the standard established for the rest of this build. Verified via script test: all three tabs render, the 40/60 split and fixed-height preview are present, all four templates produce correct default and field-driven titles, the share flow correctly blocks an empty "Specific Parishes" selection and succeeds for all three scope types, a shared document resets the active template selection afterward, and `goDocuments()` resolves to the correct URL from every one of the other 6 Diocese screens |

- **Parishioner-side audit (post-build)**: same method as the Parish and Diocese audits — every `toast()` call checked for whether it represents a missing feature vs. a simulated micro-action, cross-referenced against the Nav Hierarchy spec, and the bottom nav specifically traced on every screen (not just the Dashboard). Found and fixed four things. **(1)** Two entire spec sections, Donate and Me, were completely unbuilt — not stubs, just absent — now built (see their entries above). **(2)** A genuinely misleading pattern, worse than a placeholder: on every hub/list screen *other* than the Dashboard, "Give" and "Menu" silently redirected to the Dashboard instead of doing anything — the Dashboard was at least honest about not being ready (toast), while every other screen looked like it was just broken. Fixed by precisely targeting each button via its distinctive SVG icon path as a regex anchor (since the onclick attribute was identical across Home/Give/Menu, a naive string replace would have touched the wrong button) across all 8 affected files, verified afterward that Home's link was untouched and Give/Menu now point to the real hubs everywhere. **(3)** Two Dashboard "Pay Now" buttons that had never worked were wired to genuinely matching functionality — the subscription one to the new Me hub's Membership tab, and the Mass booking one to `screen-parishioner-massbooking-mybookings.html`, which turned out to **already have a real, working Pay Now mechanism** for a specific pending booking (`BOOK-0329`) that the Dashboard tile had simply never been pointed at. **(4)** Three Dashboard placeholders were deliberately left as-is — the hero event's "View Details," "Opening Parish Profile," and "View all announcements" — because no screen anywhere actually represents an event-detail page, a parishioner-facing parish profile, or an announcements list; linking them to the nearest approximation would have been worse than an honest placeholder, the same judgment applied to the Diocese Dashboard's unmatched Action Center tiles earlier.
